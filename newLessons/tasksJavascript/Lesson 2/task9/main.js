// Quyidagi gap to’g’rimi? A, B va C sonlari berilgan. Tomonlari A, B, 
// va C bo’lgan uchburchak to’g’ri burchakli uchburchak.

let a=3, b=4, c=5;

let javob = Math.pow(a,2) + Math.pow(b,2) == Math.pow(c,2)
console.log(javob);